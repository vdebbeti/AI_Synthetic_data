[project]
name = "sdtm-adam-compiler-blueprint"
version = "0.1.0"
description = "Blueprint scaffold for robust SDTM/ADaM spec-to-code compiler"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

